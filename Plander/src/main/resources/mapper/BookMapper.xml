<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.bit.mapper.BookMapper">
	
	<!-- 좌석 조회 -->
	<select id="seat" resultType="com.bit.domain.SeatsVO">
		SELECT * FROM SEATS WHERE BR_IDX = #{br_idx} AND SCT_IDX = #{sct_idx}
	</select>
	
	<!-- 남은 좌석 수 확인 -->
	<select id="seatCnt" resultType="int">
		SELECT COUNT(S_COL) FROM SEATS WHERE BR_IDX = #{br_idx} AND SCT_IDX = #{sct_idx}
	</select>
	

</mapper>



